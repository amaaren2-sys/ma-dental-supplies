# تشغيل MA Dental Supplies على GitHub Pages + Supabase

1. في Supabase افتح SQL Editor وشغّل كل محتوى `supabase-schema.sql` مرة واحدة.
2. من Supabase > Authentication > Users أنشئ مستخدم Admin بالبريد وكلمة المرور التي تريدها.
3. افتح `public/config.js` وضع:
   - Project URL
   - anon / publishable key
   - رقم WhatsApp بصيغة 9639XXXXXXXX
4. ارفع الملفات إلى GitHub مع الحفاظ على مجلد `public` أو انشر محتويات `public` كجذر GitHub Pages.
5. فعّل GitHub Pages من Settings > Pages واختر Deploy from a branch ثم `main` ومجلد `/public` إذا كان متاحًا في إعدادات المستودع. إذا لم يظهر اختيار `/public`، اجعل `public` هو جذر الموقع في المستودع.
6. افتح `admin.html` وسجّل الدخول بحساب Supabase الذي أنشأته.

الطلبات العامة تستخدم دالة Supabase `create_order` لتسجيل الطلب وخصم المخزون بشكل ذري، بينما عمليات الإدارة تتطلب تسجيل دخول Supabase.

ملاحظة: مفتاح anon/publishable مصمم للاستخدام في الواجهة مع RLS. لا تضع service_role key داخل GitHub.


### صور المنتجات
لوحة الإدارة تدعم الآن اختيار صورة من الكمبيوتر ورفعها تلقائيًا إلى Supabase Storage في bucket باسم `product-images`. يجب تشغيل الجزء الخاص بـ Storage في `supabase-schema.sql` مرة واحدة. الصور عامة للعرض داخل المتجر، والرفع/التعديل/الحذف متاح للمستخدمين المسجلين في Supabase فقط.


## النسخة الاحترافية v2

تتضمن النسخة:
- متجر متجاوب للموبايل والكمبيوتر.
- Supabase Database + Storage.
- رفع صور المنتجات من لوحة التحكم.
- بحث وفلاتر وترتيب وPagination للكتالوج الكبير.
- سلة مشتريات وحفظها محليًا.
- طلبات حقيقية محفوظة في Supabase مع خصم المخزون بشكل آمن.
- أرقام طلبات متسلسلة MA-1, MA-2...
- صفحة تأكيد الطلب وفتح WhatsApp بعد التسجيل.
- صفحة تتبع الطلب باستخدام رقم الطلب + الهاتف.
- لوحة تحكم للمنتجات والطلبات والمخزون والحالات.
- حذف الطلبات والمنتجات.
- تصدير واستيراد المنتجات CSV، مناسب لإدارة مئات المنتجات.
- مفضلة المنتجات.
- أسئلة شائعة.
- استعادة كلمة مرور لوحة التحكم.

### مهم قبل الرفع
احتفظ بملف `config.js` الحالي لديك لأنه يحتوي إعدادات Supabase العاملة. ارفع باقي الملفات واستبدل الملفات القديمة. ثم شغّل ملف `supabase-schema.sql` في Supabase SQL Editor مرة واحدة.
